inherit: base.yaml
experiment: E9
description: ROCKET random-convolution baseline on the raw series under subject-wise vs record-wise CV (record-wise is the object of study).
cohorts: [depresjon, psykose, hyperaktiv]
splitters: [subject_wise, record_wise]
seeds: [0, 1, 2]
n_kernels: 500
n_boot: 300
